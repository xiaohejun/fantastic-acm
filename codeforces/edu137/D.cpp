#include <bits/stdc++.h>

using namespace std;

typedef long long LL;

#ifdef ONE
#include "algo/debug.h"
#else
#define debug(...) 42
#endif

string s;
int n;


void solve()
{
    dbg((1 << 20))
}

int main(){
    freopen("in.txt", "r", stdin);
    ios::sync_with_stdio(0); cin.tie(0);
    while (cin >> n) {
        solve();
    }
    return 0;
}
